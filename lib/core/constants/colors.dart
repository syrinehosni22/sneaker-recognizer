import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.blue;
  static const primaryButton = Color.fromARGB(255, 230, 135, 253);
  static const background = Colors.white;
  static const button = Colors.blueAccent;
}
